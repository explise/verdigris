# Rust build output. `bench/loadgen` is excluded from the workspace, so it builds
# into its own target dir that the anchored rule above would miss.
/target
/bench/loadgen/target

# Local data written by the default (offline) storage backend
/data

# Local overrides
/config/local.toml
.env

# MkDocs rendered output (the public site is published from CI, not committed)
/site
